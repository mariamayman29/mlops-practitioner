services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: prodml-api:0.1.0
    ports:
      - "8000:8000"
    volumes:
      - ../models:/app/models
    environment:
      - MODEL_PATH=/app/models/baseline.onnx
    restart: unless-stopped
